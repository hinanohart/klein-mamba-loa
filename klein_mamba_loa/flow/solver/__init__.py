"""ODE solver wrapper (`torchcfm`).

S1 status: **placeholder**. The solver wrapper is a thin facade over
`torchcfm`'s ODE integrators; concrete bindings land at S3 alongside
the toy 3-run training harness.
"""
