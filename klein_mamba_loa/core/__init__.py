"""Tensor / dtype / device utilities.

S1 status: **empty package**. The directory exists so the README and
`docs/ARCHITECTURE.md` module-boundary table point at a real namespace,
but no symbols are exported until S2 lands the dtype-aware helpers.

Importing `klein_mamba_loa.core` is safe and side-effect free.
"""
